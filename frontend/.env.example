# Example Environment Variables for Frontend

# URL of the backend API for production (e.g. https://api.yourdomain.com/api)
# For local dev, Vite proxy (`/api`) takes precedence automatically
VITE_API_URL=http://localhost:5000/api
